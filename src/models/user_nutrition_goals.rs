/*
    user_id INTEGER PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
    bmr FLOAT NOT NULL,
    tdee FLOAT NOT NULL,
    calorie_goal FLOAT NOT NULL,
    protein_goal FLOAT,
    carbs_goal FLOAT,
    fats_goal FLOAT,
*/
